export const actions = {
  create: async ({ cookies, request }) => {
    // request.text().then((res) => {console.log(res);});
    // request.formData().then(res => console.log(res.));
    // let data = await request.formData();
    // let title = data.get('title');
    console.log(`CREATE`);
  }
};